language: generic
sudo: required

services:
    - docker

before_install:
    - docker build -t dikshagupta04/docker-react -f Dockerfile.dev

script:
    - docker run -re CI=true dikshagupta04/docker-react npm run test
